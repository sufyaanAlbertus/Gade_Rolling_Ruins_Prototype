using UnityEngine;

/// <summary>

public class GameManagerBootstrap : MonoBehaviour
{
    // GameManager.Awake() already handles the singleton pattern.
    // This script is intentionally empty — the prefab setup is the whole point.
    // Having this script on the prefab is just a reminder of the pattern.
}
